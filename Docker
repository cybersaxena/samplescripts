sudo docker login -u <> -p <>
sudo docker build -t autoscaler-mongo:$BUILD_NUMBER .
sudo docker tag autoscaler-mongo:$BUILD_NUMBER ssaxena/autoscaler-mongo:$BUILD_NUMBER
sudo docker push ssaxena/autoscaler-mongo:$BUILD_NUMBER

